	        	<div class="form-group">
		        	<label for="lib_dest">Destination</label>
		        	<input type="text" class="form-control" name="lib_dest" id="lib_dest">
	        	</div>
{{--
	        	<div class="form-group">
	        		<label for="idgareagence">Gare</label>
				<select name="idgareagence" id="idgareagence" class="form-control">
					@foreach($gares as $gare)
					<option value="{{$gare->id}}">{{$gare->zone}}</option>
						@endforeach
				</select>
				</div>--}}
